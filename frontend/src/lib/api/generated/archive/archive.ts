/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  ArchiveMutationBody,
  ArchivePacingStatusResponse,
  ArchiveReportResponse,
  ArchiveStatusResponse,
  GetArchiveReportParams,
  ListArchiveStatusParams,
} from "../models";

import { orvalFetch } from "../../runtime.ts";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> = (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<{ [Q in P]: T[P] }, { -readonly [Q in P]: T[P] }, P>;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (k: infer I) => void ? I : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object ? NonReadonly<NonNullable<T[P]>> : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getListArchivePacingUrl = () => {
  return `/archive/pacing`;
};

/**
 * @summary List archive hydration pacing per provider credential
 */
export const listArchivePacing = async (
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ArchivePacingStatusResponse[]> => {
  return orvalFetch<ArchivePacingStatusResponse[]>(getListArchivePacingUrl(), {
    ...options,
    method: "GET",
  });
};

export const getPauseArchivesUrl = () => {
  return `/archive/pause`;
};

/**
 * @summary Pause historical archives
 */
export const pauseArchives = async (
  archiveMutationBody: NonReadonly<ArchiveMutationBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ArchiveStatusResponse[]> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<ArchiveStatusResponse[]>(getPauseArchivesUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(archiveMutationBody),
  });
};

export const getGetArchiveReportUrl = (params: GetArchiveReportParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["repo"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/archive/report?${stringifiedParams}` : `/archive/report`;
};

/**
 * @summary Get historical archive activity report
 */
export const getArchiveReport = async (
  params: GetArchiveReportParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ArchiveReportResponse> => {
  return orvalFetch<ArchiveReportResponse>(getGetArchiveReportUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getStartArchivesUrl = () => {
  return `/archive/start`;
};

/**
 * @summary Start historical archives
 */
export const startArchives = async (
  archiveMutationBody: NonReadonly<ArchiveMutationBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ArchiveStatusResponse[]> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<ArchiveStatusResponse[]>(getStartArchivesUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(archiveMutationBody),
  });
};

export const getListArchiveStatusUrl = (params?: ListArchiveStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["repo"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/archive/status?${stringifiedParams}` : `/archive/status`;
};

/**
 * @summary List historical archive status
 */
export const listArchiveStatus = async (
  params?: ListArchiveStatusParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ArchiveStatusResponse[]> => {
  return orvalFetch<ArchiveStatusResponse[]>(getListArchiveStatusUrl(params), {
    ...options,
    method: "GET",
  });
};
