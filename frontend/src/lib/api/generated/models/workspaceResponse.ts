/**
 * Generated by Orval. Do not edit manually.
 */
import type { RepoRefResponse } from "./repoRefResponse.ts";
import type { WorkspaceKataMetadata } from "./workspaceKataMetadata.ts";
import type { WorkspaceResponseAgentState } from "./workspaceResponseAgentState.ts";
import type { WorkspaceResponseEnrichmentStatus } from "./workspaceResponseEnrichmentStatus.ts";
import type { WorkspaceResponseMrHeadRepoKind } from "./workspaceResponseMrHeadRepoKind.ts";

export interface WorkspaceResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Hook-reported aggregate state for live agent sessions. Omitted when no live session has reported lifecycle state. */
  agent_state?: WorkspaceResponseAgentState;
  /** UTC timestamp of the hook report that produced agent_state. */
  agent_state_updated_at?: string;
  associated_pr_number?: number;
  /** True when the current branch has an origin upstream configured but its local remote-tracking ref is absent; clients may offer Push so branch sync can verify or create the remote branch. */
  branch_upstream_missing?: boolean;
  commits_ahead?: number;
  commits_behind?: number;
  /** True when this response represents a workspace newly created by this request; absent when an existing workspace was returned or on reads. */
  created?: boolean;
  created_at: string;
  /** Combined error from the most recent reconciliation attempt; populated component fields may still contain last-known-good values. */
  enrichment_error?: string;
  /** Oldest successful refresh time across the populated enrichment components. */
  enrichment_refreshed_at?: string;
  /** Aggregate git-state and tmux-activity reconciliation status. Failed responses retain last-known-good component fields when available. */
  enrichment_status: WorkspaceResponseEnrichmentStatus;
  error_message?: string;
  git_head_ref: string;
  id: string;
  item_key: string;
  item_last_activity_at?: string;
  item_number: number;
  item_type: string;
  kata?: WorkspaceKataMetadata;
  mr_additions?: number;
  mr_ci_status?: string;
  mr_deletions?: number;
  /** Set only for pull_request workspaces: same_repo when the PR head is confirmed to be in the base repo, fork when it is a confirmed fork clone, unknown when repository identity could not be resolved. */
  mr_head_repo_kind?: WorkspaceResponseMrHeadRepoKind;
  mr_is_draft?: boolean;
  mr_review_decision?: string;
  mr_state?: string;
  mr_title?: string;
  platform_host: string;
  repo: RepoRefResponse;
  repo_name: string;
  repo_owner: string;
  status: string;
  tmux_activity_source: string;
  /** @nullable */
  tmux_last_output_at: string | null;
  tmux_pane_title?: string;
  tmux_session: string;
  tmux_working: boolean;
  /** True when the worktree has staged, unstaged, or untracked changes; false when clean; omitted until git-state enrichment completes. */
  worktree_dirty?: boolean;
  worktree_path: string;
}
