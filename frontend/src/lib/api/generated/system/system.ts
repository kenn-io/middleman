/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  CompleteFilesystemPathParams,
  FilesystemCompleteOutputBody,
  FilesystemValidateRepoOutputBody,
  ListUserRepositoriesOutputBody,
  ListUserRepositoriesParams,
  ProblemError,
  StoreTerminalPasteImageHeaders,
  StreamEventsParams,
  TelemetryEventInputBody,
  TelemetryEventResponse,
  TerminalPasteImageOutputBody,
  ToolingStatusBody,
  ValidateFilesystemRepoParams,
  VersionOutputBody,
} from "../models";

import { orvalFetch } from "../../runtime.ts";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> = (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<{ [Q in P]: T[P] }, { -readonly [Q in P]: T[P] }, P>;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (k: infer I) => void ? I : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object ? NonReadonly<NonNullable<T[P]>> : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getStreamEventsUrl = (params?: StreamEventsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/events?${stringifiedParams}` : `/events`;
};

/**
 * @summary Stream server events
 */
export const streamEvents = async (
  params?: StreamEventsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ProblemError> => {
  return orvalFetch<ProblemError>(getStreamEventsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getCompleteFilesystemPathUrl = (params: CompleteFilesystemPathParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/filesystem/complete?${stringifiedParams}` : `/filesystem/complete`;
};

/**
 * @summary Complete a local filesystem path
 */
export const completeFilesystemPath = async (
  params: CompleteFilesystemPathParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<FilesystemCompleteOutputBody> => {
  return orvalFetch<FilesystemCompleteOutputBody>(getCompleteFilesystemPathUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getValidateFilesystemRepoUrl = (params: ValidateFilesystemRepoParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/filesystem/validate-repo?${stringifiedParams}` : `/filesystem/validate-repo`;
};

/**
 * @summary Resolve a path to a repository root
 */
export const validateFilesystemRepo = async (
  params: ValidateFilesystemRepoParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<FilesystemValidateRepoOutputBody> => {
  return orvalFetch<FilesystemValidateRepoOutputBody>(getValidateFilesystemRepoUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getListUserRepositoriesUrl = (params?: ListUserRepositoriesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/platform/user-repositories?${stringifiedParams}`
    : `/platform/user-repositories`;
};

/**
 * @summary List the authenticated platform CLI user's repositories
 */
export const listUserRepositories = async (
  params?: ListUserRepositoriesParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ListUserRepositoriesOutputBody> => {
  return orvalFetch<ListUserRepositoriesOutputBody>(getListUserRepositoriesUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getCaptureTelemetryEventUrl = () => {
  return `/telemetry/events`;
};

/**
 * @summary Capture telemetry event
 */
export const captureTelemetryEvent = async (
  telemetryEventInputBody: NonReadonly<TelemetryEventInputBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<TelemetryEventResponse> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<TelemetryEventResponse>(getCaptureTelemetryEventUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(telemetryEventInputBody),
  });
};

export const getStoreTerminalPasteImageUrl = () => {
  return `/terminal/paste-image`;
};

/**
 * @summary Store a browser clipboard image for terminal paste
 */
export const storeTerminalPasteImage = async (
  storeTerminalPasteImageBody: Blob,
  headers?: StoreTerminalPasteImageHeaders,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<TerminalPasteImageOutputBody> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<TerminalPasteImageOutputBody>(getStoreTerminalPasteImageUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/octet-stream", ...headers, ...getHeaders(options?.headers) },
    body: storeTerminalPasteImageBody,
  });
};

export const getGetToolingStatusUrl = () => {
  return `/tooling-status`;
};

/**
 * @summary Report git/gh/glab CLI availability and auth
 */
export const getToolingStatus = async (options?: Parameters<typeof orvalFetch>[1]): Promise<ToolingStatusBody> => {
  return orvalFetch<ToolingStatusBody>(getGetToolingStatusUrl(), {
    ...options,
    method: "GET",
  });
};

export const getGetVersionUrl = () => {
  return `/version`;
};

/**
 * @summary Get server version
 */
export const getVersion = async (options?: Parameters<typeof orvalFetch>[1]): Promise<VersionOutputBody> => {
  return orvalFetch<VersionOutputBody>(getGetVersionUrl(), {
    ...options,
    method: "GET",
  });
};
