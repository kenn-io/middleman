/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  ActivityAuthorsResponse,
  ActivityResponse,
  AgentHookResponse,
  HookEvent,
  ListActivityAuthorsParams,
  ListActivityParams,
  ListActivityThreadEventsParams,
  ListNotificationsParams,
  NotificationBulkInputBody,
  NotificationBulkResponse,
  NotificationsResponse,
  ReceiveAgentHookHeaders,
  ReceiveAgentHookPathParameters,
} from "../models";

import { orvalFetch } from "../../runtime.ts";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> = (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<{ [Q in P]: T[P] }, { -readonly [Q in P]: T[P] }, P>;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (k: infer I) => void ? I : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object ? NonReadonly<NonNullable<T[P]>> : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getListActivityUrl = (params?: ListActivityParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/activity?${stringifiedParams}` : `/activity`;
};

/**
 * @summary List activity
 */
export const listActivity = async (
  params?: ListActivityParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ActivityResponse> => {
  return orvalFetch<ActivityResponse>(getListActivityUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getListActivityAuthorsUrl = (params?: ListActivityAuthorsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/activity/authors?${stringifiedParams}` : `/activity/authors`;
};

/**
 * @summary List activity authors
 */
export const listActivityAuthors = async (
  params?: ListActivityAuthorsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ActivityAuthorsResponse> => {
  return orvalFetch<ActivityAuthorsResponse>(getListActivityAuthorsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getListActivityThreadEventsUrl = (params?: ListActivityThreadEventsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/activity/thread-events?${stringifiedParams}` : `/activity/thread-events`;
};

/**
 * @summary List activity thread events
 */
export const listActivityThreadEvents = async (
  params?: ListActivityThreadEventsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ActivityResponse> => {
  return orvalFetch<ActivityResponse>(getListActivityThreadEventsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getReceiveAgentHookUrl = ({ agent }: ReceiveAgentHookPathParameters) => {
  return `/agent-hooks/${encodeURIComponent(String(agent))}`;
};

/**
 * @summary Receive agent lifecycle hook
 */
export const receiveAgentHook = async (
  { agent }: ReceiveAgentHookPathParameters,
  hookEvent: NonReadonly<HookEvent>,
  headers?: ReceiveAgentHookHeaders,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<AgentHookResponse> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<AgentHookResponse>(getReceiveAgentHookUrl({ agent }), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...headers, ...getHeaders(options?.headers) },
    body: JSON.stringify(hookEvent),
  });
};

export const getListNotificationsUrl = (params?: ListNotificationsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notifications?${stringifiedParams}` : `/notifications`;
};

/**
 * @summary List notifications
 */
export const listNotifications = async (
  params?: ListNotificationsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<NotificationsResponse> => {
  return orvalFetch<NotificationsResponse>(getListNotificationsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getMarkNotificationsDoneUrl = () => {
  return `/notifications/done`;
};

/**
 * @summary Mark notifications done
 */
export const markNotificationsDone = async (
  notificationBulkInputBody: NonReadonly<NotificationBulkInputBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<NotificationBulkResponse> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<NotificationBulkResponse>(getMarkNotificationsDoneUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(notificationBulkInputBody),
  });
};

export const getMarkNotificationsReadUrl = () => {
  return `/notifications/read`;
};

/**
 * @summary Mark notifications read
 */
export const markNotificationsRead = async (
  notificationBulkInputBody: NonReadonly<NotificationBulkInputBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<NotificationBulkResponse> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<NotificationBulkResponse>(getMarkNotificationsReadUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(notificationBulkInputBody),
  });
};

export const getMarkNotificationsUndoneUrl = () => {
  return `/notifications/undone`;
};

/**
 * @summary Mark notifications undone
 */
export const markNotificationsUndone = async (
  notificationBulkInputBody: NonReadonly<NotificationBulkInputBody>,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<NotificationBulkResponse> => {
  const getHeaders = (h?: NonNullable<RequestInit["headers"]>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return orvalFetch<NotificationBulkResponse>(getMarkNotificationsUndoneUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(notificationBulkInputBody),
  });
};
